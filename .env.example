# Copy to .env (or spike/.env for the Stage 0 spike) and fill in. Never commit the real file.
SERPER_API_KEY=
BRAVE_API_KEY=

# Neon Postgres. Pooled URL for the app; direct (unpooled) URL for migrations and the pipeline.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
